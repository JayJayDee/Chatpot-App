// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "chat.chatpot"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
